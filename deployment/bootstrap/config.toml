# Bootstrap Node Configuration
# Laptop 1 - Friend A's House
#
# This is the network root node. All other nodes connect to this one first.
# IMPORTANT: This node must be publicly accessible (port forwarding or ngrok required)

listen_addr = "0.0.0.0"
listen_port = 9000
bootstrap_nodes = []
accept_relay = true
max_peers = 100
data_dir = "./data"
verbose = false
